# Skopiuj jako `.env` (plik jest w .gitignore).

PORT=8787

# Baza gry — te same dane co w sf555/.env
SF_DB_HOST=localhost
SF_DB_PORT=3306
SF_DB_NAME=sf555
SF_DB_USER=sf555_app
SF_DB_PASS=zmien-to-haslo

# Stary backend PHP. Akcje jeszcze nieprzeniesione sa tam przekazywane,
# dzieki czemu gra dziala przez caly czas migracji.
# Zostaw puste, zeby wylaczyc przekazywanie.
SF_LEGACY_URL=http://localhost:8080/req.php
