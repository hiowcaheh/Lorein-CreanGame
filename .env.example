# Konfiguracja docker compose. Skopiuj jako `.env` (plik jest w .gitignore).
# Hasla wygeneruj losowo, np.:  openssl rand -base64 24

DB_ROOT_PASSWORD=zmien-to-haslo-roota
DB_USER=sf555_app
DB_PASSWORD=zmien-to-haslo-aplikacji
